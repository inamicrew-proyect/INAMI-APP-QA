// Archivo de ejemplo para configuración de PM2
// Copia este archivo a ecosystem.config.js y ajusta los valores según tu entorno

module.exports = {
  apps: [{
    name: 'inami-qa',
    script: '.next/standalone/server.js',
    // ⚠️ IMPORTANTE: Reemplaza con la ruta completa de tu proyecto
    cwd: '/home/tu-usuario/INAMI-APP-QA',
    instances: 1,
    exec_mode: 'fork',
    env: {
      NODE_ENV: 'production',
      PORT: 3001
    },
    // ⚠️ IMPORTANTE: Ajusta las rutas de logs según tu usuario
    error_file: '/home/tu-usuario/.pm2/logs/inami-qa-error.log',
    out_file: '/home/tu-usuario/.pm2/logs/inami-qa-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    max_memory_restart: '1G',
    watch: false,
    ignore_watch: ['node_modules', '.next', '.git', '.env.local']
  }]
}

// INSTRUCCIONES:
// 1. Copia este archivo: cp ecosystem.config.js.example ecosystem.config.js
// 2. Edita ecosystem.config.js y reemplaza:
//    - /home/tu-usuario/ con tu ruta real de usuario
//    - Ajusta el nombre de la app si lo deseas
//    - Ajusta el puerto si es necesario (3001 por defecto)
// 3. Inicia con PM2: pm2 start ecosystem.config.js
// 4. Guarda la configuración: pm2 save
